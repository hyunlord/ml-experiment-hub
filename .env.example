# =============================================================================
# ML Experiment Hub - Environment Configuration
# Copy to .env and update paths for your system.
# =============================================================================

# ---------------------------------------------------------------------------
# Host paths (mounted into Docker containers as volumes)
# ---------------------------------------------------------------------------

# Directory containing ML project repositories (e.g., vlm_quantization/)
PROJECTS_DIR=/home/user/projects

# Training data directory (COCO, AIHub, etc.)
DATA_DIR=/home/user/data

# Checkpoint storage directory
CHECKPOINT_DIR=/home/user/checkpoints

# ---------------------------------------------------------------------------
# DGX Spark example:
# ---------------------------------------------------------------------------
# PROJECTS_DIR=/home/nvidia/vlm_quantization
# DATA_DIR=/home/nvidia/vlm_quantization/data
# CHECKPOINT_DIR=/home/nvidia/vlm_quantization/checkpoints

# ---------------------------------------------------------------------------
# Backend settings (usually set in docker-compose, not here)
# Uncomment to override for local (non-Docker) development.
# ---------------------------------------------------------------------------
# DATABASE_URL=sqlite+aiosqlite:///./ml_experiments.db
# CORS_ORIGINS=["*"]
# LOG_LEVEL=INFO
# LOG_DIR=./logs
